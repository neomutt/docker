# Create Centos Docker images containing all the packages required for a full build of NeoMutt.
name: Centos
on:
  push:
  schedule:
    - cron: '0 3 1 * *'
      # 1st of the month at 03:00

jobs:
  build:
    strategy:
      matrix:
        cfg:
          - name: Centos 7
            key: 7
          - name: Centos 8
            key: 8

    name: Build ${{ matrix.cfg.name }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build ${{ matrix.cfg.name }}
        uses: pmorelli92/github-container-registry-build-push@2.0.0
        with:
          github-push-secret: ${{secrets.GITHUB_TOKEN}}
          docker-image-name: centos
          docker-image-tag: ${{ matrix.cfg.key }}
          dockerfile-path: ./Dockerfile.centos${{ matrix.cfg.key }}

